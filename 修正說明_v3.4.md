# 日語練習所 v3.4 修正說明

修的是朋友回報的「中文」與「連播」。這兩個功能之前修不好，主要原因是**它們互相咬在一起**，而且失敗是間歇性的——重整一次症狀就變了，所以很容易誤判成「已經好了」。

這次先架了無頭瀏覽器 + 假的 Azure 語音伺服器，把每一段實際播出的音檔跟當下畫面上的卡片對起來記錄，**先把失敗重現出來**，再動手改。下面每一條都有測試結果，不是讀 code 猜的。

---

## 一、找到的問題（都能穩定重現）

### 1. 連播中按「上一張／下一張」→ 連播永久停止，但開關還顯示「開」🔴 最嚴重

實測 8 次，全部 100% 重現：

| 動作 | 修正前 8 秒內還播幾段 | 修正後 |
|---|---|---|
| 什麼都不做 | 19 | 18 |
| 按「下一張」 | **0** | 18 |
| 按「上一張」 | **0** | 18 |
| 按「下一張」（中日開） | **1** | 13 |
| 按「下一張」（×3 開） | **0** | 17 |

原因：`nextCard()` / `prevCard()` 會呼叫 `_stopAudio()`，讓內部的 `_speakVersion` 前進、作廢所有進行中的播放鏈——但**沒有任何程式碼負責把連播重新接上**。而 `state.isLooping` 還是 `true`、開關還是打勾，所以使用者看到的是「開關明明開著，就是不播了」。

這一條最能解釋「輪播修不好」：連播時聽到不想練的字，很自然會按下一張，一按就死。

修法：新增 `_loopResumeIfNeeded()`，換卡、評分、切篩選、從題庫點卡之後都把連播接回去。

### 2. 連播 + 智慧排序 + 中途評分 → 語音唸的字和畫面上的卡不一樣

實測錄到（修正前）：

```
+3.1s 唸「中国人」 但畫面顯示「日本人」
+3.9s 唸「傘」    但畫面顯示「えっ」
      卡片編號從 9 直接跳到 12
```

原因：`practiceItems()` 每次被呼叫都**即時重新排序 1041 張卡**，而排序依據是 `Date.now()` 和 SRS 紀錄。一輪播放中它會被呼叫十幾次（`loadCurrentCard`、`getCurrentItem`、`_prefetchNext`、`_loopNextCard`…）。只要評分一張卡，那張卡就從「到期」變成「未到期」、整個清單當場重排——先前預抓好的音檔就對到別張卡去了。

修法：改成快取佇列，只在明確事件（切篩選、切排序、增刪卡片、重置紀錄、重新載入）時重建，播放過程中絕對不動。重建時還會盡量讓你停在同一張卡上。

### 3. 中文語音失敗時完全靜默

`speakChinese()` 沒有檢查 `res.ok`，`catch` 也什麼都不做。日文失敗會跳提示，中文失敗**一點訊息都沒有**——聽起來就是「中文有時候就是不會唸，不知道為什麼」。

修法：加上狀態碼檢查與錯誤提示。現在會顯示例如 `中文語音失敗：中文語音請求失敗 401`（測試已驗證）。

### 4. 播中文時按鈕顯示「▶ 單字」而不是「■ 停止」

`speakChinese()` 沒更新按鈕狀態，使用者以為已經停了、又去按一次，反而把播放打斷。修法：補上播放中／結束的 UI 切換。

### 5. 停止後殘留的中文會唸到別張卡的意思

實測錄到：畫面顯示「課長」，中文卻唸出「店員」。

原因：`_pfDone` 這個回呼**完全沒有版本守衛**。按停止或換卡後它仍會執行，並讀取「當下」的 `state.cnMode`，把上一張卡的中文意思唸出來。修法：`_pfDone` 及三處中文呼叫點全部加上版本守衛。

### 6. 打開「連播」開關不會開始播；關掉也不會馬上停

原本 `toggleLoop()` 只改變數，要另外再按一次 ▶ 才會播。修法：開＝立刻連播，關＝立刻停止。切換本身就是使用者手勢，順便解鎖 iOS 音訊。

### 7. iOS 音訊解鎖有競態，第一段可能無聲

原本用 `volume = 0` 播無聲檔，要等 `play()` 的 promise resolve 才設回 `1`。如果語音很快回來、在 resolve 之前就換了音源，**第一段會完全沒聲音**。另外原本只在「已開連播」時解鎖，若先按播放、之後才開連播，iOS 會直接擋掉整條連播鏈。

修法：改用 `muted` 並在 `play()` 後同步解除，不留競態窗口；任何播放手勢都解鎖。

### 8. 連播／中日／×3／語速／JLPT 篩選 重整就全部歸零

朋友每次打開都要重設一遍。而且更麻煩的是：**Safari 的返回／bfcache 會自行還原 checkbox 的勾選，但 JS 的 state 是重新初始化的 `false`** ——開關看起來是開的、實際沒作用。這正是「開了沒反應」最典型的來源。

修法：五項設定都存進 localStorage；並在啟動、`DOMContentLoaded`、`pageshow`（含 bfcache 還原）三個時機都強制把 checkbox 對齊 state。

### 9. 評分按鈕連點兩次會跳過一張卡

評分後要等 650ms 才換卡，這段時間內再按一次會對同一張卡評兩次分、多前進一張——中間那張卡不會出現也不會發音。手機上很容易誤觸。修法：加上防連點。

---

## 二、測試結果

**連播存活測試**（動作後 8 秒內播不到 3 段就算連播已死）

```
✅ 基準：什麼都不做              ✅ 評分「記得」
✅ 按「下一張」                  ✅ 切 JLPT 篩選 N5
✅ 按「上一張」                  ✅ 從題庫點某張卡練習
✅ 按「下一張」（中日開）           ✅ 切到題庫頁再回練習
✅ 按「下一張」（×3 開）
```

**音畫同步測試**（比對每段實際播出的音檔 vs 當下畫面卡片）

| 情境 | 播出段數 | 錯位 |
|---|---|---|
| 只連播 | 27 | 0 ✅ |
| 連播＋中日 | 25 | 0 ✅ |
| 連播＋×3 | 35 | 0 ✅ |
| 連播＋中日＋×3 全開 | 33 | 0 ✅ |
| 只中日不連播 | 2 | 0 ✅ |
| 連播＋智慧排序 | 27 | 0 ✅ |
| 中日播放中換卡 | 16 | 0 ✅ |
| 連播中評分 | 26 | 0 ✅ |

（修正前：連播中評分 2/28 錯位；中日播放中換卡只播 2 段就死）

**殘留語音測試**：四個情境全部 ✅，停止後不再冒出上一張卡的語音。

**煙霧測試**：資料完整性、六個分頁渲染、Ruby 標記、例句播放、搜尋、新增／刪除卡、JLPT 篩選、統計 —— 24 項全過，頁面錯誤 0。

---

## 三、其他

- **1041 張卡片資料完全沒動**（MD5 比對一致：`3d8dbde9acb0b19c66082950d55c5e05`）
- 兩個 `<script>` 區塊都通過 `node --check` 語法檢查
- 版號 v3.3 → **v3.4**，方便確認朋友拿到的是新版
- 修改集中在音訊與佇列邏輯，共 20 處，每處都有中文註解說明原本錯在哪
- 學習紀錄（SRS）儲存格式沒變，朋友的進度不會遺失

## 四、還沒做的

- iOS Safari 實機驗證。測試是在無頭 Chromium 上跑的，`NotAllowedError`／背景播放這類 iOS 專屬行為只能靠程式碼推論加固，沒辦法在這裡真的重現。如果朋友主要用 iPhone，建議請他實際試一輪連播、鎖屏、再解鎖。
- 講義 PDF 的單字整合（另一件事，等這版確認沒問題再做）。
